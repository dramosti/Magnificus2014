<runtime:WindowsBase 
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:runtime="clr-namespace:HLP.Comum.View.Formularios;assembly=HLP.Comum.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Components="clr-namespace:HLP.Comum.View.Components;assembly=HLP.Comum.View"
        x:Class="HLP.Entries.View.WPF.Gerais.WinCalendario"
        Title="WinCalendario" Height="660" Width="1024">
	<DockPanel LastChildFill="True">
		<Expander DockPanel.Dock="Left" Header="Principal" IsExpanded="True" Width="485">
			<WrapPanel Orientation="Vertical">				
				<Components:HlpTextBox 
					Validation.ErrorTemplate="{StaticResource exceptionComp}"
					Text="{Binding currentModel.idCalendario, Mode=TwoWay, NotifyOnValidationError=True, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
					IsEnabled="{Binding bIsEnabled}"
					Height="27" Margin="10,10,10,2" HorizontalAlignment="Left" Caption="Código" Help="Chave principal do registro calendário" WidthLabel="137" Width="224.333"/>
				<Components:HlpTextBox 
					Validation.ErrorTemplate="{StaticResource exceptionComp}"
					Text="{Binding currentModel.xNome, Mode=TwoWay, NotifyOnValidationError=True, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
					IsEnabled="{Binding bIsEnabled}"
					Height="27" Margin="10,0,10,2" HorizontalAlignment="Left" Caption="Nome" Help="Insira resumidamente o nome do calendário" WidthLabel="137" Width="400"/>
				<Components:HlpTextBox 
					Validation.ErrorTemplate="{StaticResource exceptionComp}"
					Text="{Binding currentModel.xDescricao, Mode=TwoWay, NotifyOnValidationError=True, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
					IsEnabled="{Binding bIsEnabled}"
					Height="27" Margin="10,0,10,2" HorizontalAlignment="Left" Caption="Descrição " Help="Descrição do calendário" WidthLabel="137" Width="400"/>
				<Components:HlpPesquisa 
					Validation.ErrorTemplate="{StaticResource exceptionComp}"
					Text="{Binding currentModel.idCalendarioBase, Mode=TwoWay, NotifyOnValidationError=True, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
					IsEnabled="{Binding bIsEnabled}"
					Height="27" Margin="10,0,10,2" HorizontalAlignment="Left" Caption="Calendário Base" Help="Informar o calendário base que esse novo calendário irá usar, essa opção desabilitará todos os recurso de geração de datas e horas e usará como base o calendário indicado, para fazer a alteração nesse calendário é preciso alterar o calendário base, tomar cuidado que ao alterar o calendário base será alterados todos os calendários que o utilizam." WidthLabel="137" Width="400" FieldPesquisa="ID" TableView="vwCalendario">
					<Components:HlpPesquisa.Items>
									<System:String>xNome</System:String>
									<System:String>xDescricao</System:String>																		
								</Components:HlpPesquisa.Items>
					</Components:HlpPesquisa>
				<Button Content="Gerar" Height="27" Width="75" HorizontalAlignment="Left" Margin="147,0,10,2"/>				
			</WrapPanel>			
		</Expander>
		<Expander Header="Detalhamento" IsExpanded="True">
			<DockPanel LastChildFill="True">
				<StackPanel DockPanel.Dock="Top" Height="49" Orientation="Horizontal">
                    <Button Content="Gerar Detalhamento" Command="{Binding gerarDetalhamentoCommand}" Width="175" Margin="0,10,10,10"/>					
				</StackPanel>
				<DataGrid
					IsEnabled="{Binding bIsEnabled}"
					AutoGenerateColumns="False" 
                    SelectionUnit="CellOrRowHeader" 
                    CanUserReorderColumns="False"  
                    CanUserDeleteRows="True" >
					<DataGrid.Columns>
						<DataGridComboBoxColumn ClipboardContentBinding="{x:Null}" Header="Data" SelectedValueBinding="{x:Null}" SelectedItemBinding="{x:Null}" TextBinding="{x:Null}" Width="*"/>
						<DataGridTextColumn 
							ElementStyle="{StaticResource CellErrorStyle}"
                            Binding="{Binding Path=dHoraInicio, UpdateSourceTrigger=LostFocus, NotifyOnValidationError=True, ValidatesOnDataErrors=True}"
							Header="Hora Início" Width="150"/>
						<DataGridTextColumn 
							ElementStyle="{StaticResource CellErrorStyle}"
                            Binding="{Binding Path=dHoraTermino, UpdateSourceTrigger=LostFocus, NotifyOnValidationError=True, ValidatesOnDataErrors=True}"
							Header="Hora Final" Width="150"/>
					</DataGrid.Columns>										
				</DataGrid>
			</DockPanel>
		</Expander>
			
	</DockPanel>
</runtime:WindowsBase>
